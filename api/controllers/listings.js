const createNewListing = (req, res) => {
  // post new listing
};

const fetchAllListings = (req, res) => {
  // get necessary info from database
}

export default {
  createNewListing,
  fetchAllListings
};
